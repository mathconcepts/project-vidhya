---
id: implicit-differentiation.hook
concept_id: implicit-differentiation
atom_type: hook
bloom_level: 1
difficulty: 0
exam_ids: ["*"]
---

When y is tangled with x in an equation (like a circle x²+y²=25), you differentiate both sides treating y as a hidden function of x — like peeling both layers of a sandwich at once.

```interactive-spec
{"v":1,"kind":"simulation","title":"One formula, dy/dx = -x/y, covers the whole circle x²+y²=25","why":"dy/dx=-x/y comes from applying the chain rule to y² directly, without ever solving y=±√(25−x²) first — one formula, both branches, even at the extremes.","x_expr":"5*cos(t)","y_expr":"5*sin(t)","t_min":0,"t_max":1.5708,"duration_sec":7,"view_box":{"x_min":-0.5,"x_max":5.5,"y_min":-0.5,"y_max":5.5},"narration_steps":[{"at_progress":0.0,"focus_point":true,"text":"At (5,0), the rightmost point on x²+y²=25, differentiate both sides: 2x+2y·dy/dx=0. Predict: what happens to this equation exactly here, where y=0?","text_shaken":"At (5,0): x=5, y=0. Plug into 2x+2y·dy/dx=0 → 10+0=0? Something is off — this point needs a closer look.","text_assured":"(5,0): y=0 here, so dy/dx=-x/y divides by zero — check whether that means no tangent exists, or a vertical one."},{"at_progress":0.4097,"focus_point":true,"text":"By (4,3), further along the arc, dy/dx=-x/y=-4/3 — a clean, ordinary tangent slope, no division trouble here.","text_shaken":"At (4,3): check 4²+3²=16+9=25 ✓. dy/dx=-4/3=-1.333.","text_assured":"-4/3 at (4,3) — away from y=0, the formula behaves exactly like an ordinary derivative."},{"at_progress":0.5903,"focus_point":true,"emphasize":true,"text":"At (3,4), the correct slope is -3/4 — found by differentiating x²+y²=25 as written, never by solving for y first. A common slip: writing d/dx[y²]=2y, dropping the dy/dx factor y secretly carries as a hidden function of x.","text_shaken":"At (3,4): 2(3)+2(4)·dy/dx=0 → dy/dx=-3/4. Forgetting the dy/dx factor gives 2x+2y=0 instead — an equation with no dy/dx to solve for at all, a dead end.","text_assured":"-3/4 at (3,4). d/dx[y²]=2y·dy/dx, never 2y alone — the missing factor is the entire mechanism implicit differentiation supplies.","trap":{"text":"Students differentiate y² as 2y, treating y like an ordinary variable in x and dropping the chain-rule factor dy/dx.","avoid":"y is secretly y(x) — every y-term carries its own dy/dx factor: d/dx[y²]=2y·dy/dx, not 2y alone."}},{"at_progress":1.0,"focus_point":true,"text":"Why one formula covers the whole circle: dy/dx=-x/y comes straight from the chain rule applied to y², with no need to split into upper and lower branches. At (0,5), the very top, dy/dx=-0/5=0 — a flat tangent, exactly where the circle peaks.","text_shaken":"At (0,5): dy/dx=-0/5=0. Flat tangent at the top of the circle — matches what a peak should look like, and the same one formula produced it.","text_assured":"0 at (0,5) — the chain-rule-derived formula -x/y needs no case-split between branches; it already knows the geometry."}]}
```
