---
description: Restrict file edits to a specific directory for the session. Blocks Edit and 
---
Invoke the `freeze` skill from gstack using the Skill tool. Pass any arguments the user provided after the slash command as the skill's args.

Skill name: `freeze`
