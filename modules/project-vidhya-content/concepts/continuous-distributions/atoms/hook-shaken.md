---
# Alternative body for continuous-distributions.hook, served when the
# learner stance is `shaken`. The base file is what a steady student reads.
# See src/content/stance-variants.ts for how this is selected.
#
# Written for a student who is low on this concept and low on confidence:
# smallest true first step, concrete numbers before symbols, picture before
# formula, and the check made explicit. No praise, no reassurance, and no
# mention of how the reader might be feeling — a small win is what steadies
# someone, not being told they are struggling.
id: continuous-distributions.hook.shaken
concept_id: continuous-distributions
atom_type: hook
bloom_level: 1
difficulty: 0
exam_ids: ["*"]
variant_of: continuous-distributions.hook
for_stance: shaken
---

A bulb's lifetime is a number like $100.5$ hours — but $100.500001$ works too, and so does every value between. There are too many exact values for any single one to have positive probability: $P(X=100.5)=0$. Only a range like $P(100<X<110)$ gives a real number.
