---
description: Performance regression detection using the browse daemon. Establishes 
---
Invoke the `benchmark` skill from gstack using the Skill tool. Pass any arguments the user provided after the slash command as the skill's args.

Skill name: `benchmark`
