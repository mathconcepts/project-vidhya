---
description: Design finalization: generates production-quality Pretext-native HTML/CSS. 
---
Invoke the `design-html` skill from gstack using the Skill tool. Pass any arguments the user provided after the slash command as the skill's args.

Skill name: `design-html`
