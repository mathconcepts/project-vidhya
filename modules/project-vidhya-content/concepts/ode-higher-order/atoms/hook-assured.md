---
# Alternative body for ode-higher-order.hook, served when the learner stance
# is `assured`. The base file is what a steady student reads.
# See src/content/stance-variants.ts for how this is selected.
#
# Written for a student who already has the mechanics: terse, assumes the
# vocabulary, and spends its words on the distinctions that actually cost
# marks (degenerate cases, faster routes, common false generalisations)
# rather than re-teaching what they can already do.
id: ode-higher-order.hook.assured
concept_id: ode-higher-order
atom_type: hook
bloom_level: 1
difficulty: 0
exam_ids: ["*"]
variant_of: ode-higher-order.hook
for_stance: assured
---

An $n$th-order linear equation needs exactly $n$ independent solutions in the final answer. Losing one — most often the $xe^{rx}$ partner a repeated root demands — is the single most common way marks disappear here, not an error in finding the roots themselves.
