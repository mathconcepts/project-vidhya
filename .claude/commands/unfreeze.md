---
description: Clear the freeze boundary set by /freeze, allowing edits to all directories 
---
Invoke the `unfreeze` skill from gstack using the Skill tool. Pass any arguments the user provided after the slash command as the skill's args.

Skill name: `unfreeze`
