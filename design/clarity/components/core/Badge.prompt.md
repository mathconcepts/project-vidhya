Labels a status or category inline.

```jsx
<Badge tone="mastery">Mastered</Badge>
<Badge mono>PYQ-2019-14</Badge>
```

Sentence case only. Use `mono` when the content is an identifier or a computed number.
