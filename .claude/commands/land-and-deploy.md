---
description: Land and deploy workflow. Merges the PR, waits for CI and deploy, 
---
Invoke the `land-and-deploy` skill from gstack using the Skill tool. Pass any arguments the user provided after the slash command as the skill's args.

Skill name: `land-and-deploy`
