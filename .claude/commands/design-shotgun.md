---
description: Design shotgun: generate multiple AI design variants, open a comparison board, 
---
Invoke the `design-shotgun` skill from gstack using the Skill tool. Pass any arguments the user provided after the slash command as the skill's args.

Skill name: `design-shotgun`
