---
id: linear-independence.hook
concept_id: linear-independence
atom_type: hook
bloom_level: 1
difficulty: 0.0
exam_ids: ["*"]
---

Can a group of vectors stand alone, or are some just "copies" of others? Linear independence is how we measure whether vectors in a set are truly distinct, or whether one can be built from the others—a foundation concept that determines what vectors can form the basis of a space.

```interactive-spec
{"v":1,"kind":"simulation","title":"Sweeping v2 around v1=(2,1): independent except at two angles","x_expr":"3*cos(t)","y_expr":"3*sin(t)","t_min":0,"t_max":6.28319,"duration_sec":6,"view_box":{"x_min":-3.5,"x_max":3.5,"y_min":-3.5,"y_max":3.5},"narration_steps":[{"at_progress":0,"text":"$v_1=(2,1)$ is fixed. This traces $v_2$ sweeping a circle of radius 3. At $t=0$: $v_2=(3,0)$ — not parallel to $v_1$, so $\\{v_1,v_2\\}$ span a nonzero area: independent.","text_shaken":"$v_1=(2,1)$ stays put. At $t=0$, $v_2=(3,0)$ — pointing along the x-axis, nowhere near $v_1$'s direction. Independent.","text_assured":"The determinant $\\det[v_1\\ v_2]=2(0)-1(3)=-3\\neq0$ — nonzero area is the independence test, not appearance.","emphasize":false},{"at_progress":0.3,"text":"By $t=108°$: $v_2\\approx(-0.93,2.85)$. Still nowhere near $v_1$'s line — still independent.","text_shaken":"At $t=108°$: $v_2\\approx(-0.93,2.85)$. Different from $v_1=(2,1)$ in every way that matters — still independent.","text_assured":"The determinant here is about $6.6$ — comfortably nonzero; independence doesn't come in degrees, but the margin does vary.","emphasize":false},{"at_progress":0.574,"text":"At $t\\approx207°$: $v_2\\approx(-2.68,-1.35)$ — exactly $-1.34\\times v_1$. The two vectors point along the same line: dependent.","text_shaken":"At this instant: $v_2\\approx(-2.68,-1.35)$. Divide by $v_1=(2,1)$: same ratio in both coordinates, $-1.34$. Same line. Dependent.","text_assured":"$v_2=-1.3416\\,v_1$ here — a genuine scalar multiple, so the determinant is exactly $0$ and the pair collapses to one direction.","emphasize":true,"trap":{"text":"Students see two different-looking vectors and assume that alone proves independence; here $v_2$ is simply $-1.34\\times v_1$ — a different point, the same line, dependent.","avoid":"Test for a scalar multiple (a zero determinant), never judge independence by whether the vectors merely look different."}},{"at_progress":0.85,"text":"By $t=306°$: $v_2\\approx(1.76,-2.43)$, off $v_1$'s line again — independent. Out of the whole circle, only two angles ever caused dependence.","text_shaken":"At $t=306°$: $v_2\\approx(1.76,-2.43)$. Off the line again. Independent.","text_assured":"Dependence occurred at exactly two isolated angles across the whole sweep — a measure-zero event, not something that happens 'often.'","emphasize":false}]}
```
