---
description: Post-deploy canary monitoring. Watches the live app for console errors, 
---
Invoke the `canary` skill from gstack using the Skill tool. Pass any arguments the user provided after the slash command as the skill's args.

Skill name: `canary`
