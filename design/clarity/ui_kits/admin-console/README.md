# UI kit — owner console

Owner surface at `/owner/dashboard`, built from Part 2 of `docs/USER-JOURNEY.md`;
see `guidelines/journey-teacher-admin.md`.

- **The cohort insight leads**, not the setup checklist — the repo's bliss checkpoint 2 is the first thing on screen once there is data.
- **Setup checklist removes itself** at 5 of 5 (tick the two remaining rows to watch it go).
- **Deployment status** is four rows and four dots; green states carry no other colour.
- **No sparkline until seven days of data** — the page says how long is left instead of drawing an empty chart.
