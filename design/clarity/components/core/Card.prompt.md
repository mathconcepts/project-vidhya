Wraps a group of related content on the grey canvas.

```jsx
<Card elevated padding={24}>…</Card>
```

Only one `elevated` card per screen. Nest radii: a child inside a 20px card at 16px padding should use 8–12px, never 20.
