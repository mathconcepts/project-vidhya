---
description: Set up gbrain for this coding agent: install the CLI, initialize a 
---
Invoke the `setup-gbrain` skill from gstack using the Skill tool. Pass any arguments the user provided after the slash command as the skill's args.

Skill name: `setup-gbrain`
