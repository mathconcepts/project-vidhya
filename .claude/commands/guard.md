---
description: Full safety mode: destructive command warnings + directory-scoped edits. 
---
Invoke the `guard` skill from gstack using the Skill tool. Pass any arguments the user provided after the slash command as the skill's args.

Skill name: `guard`
