{
  "$comment": "Grandfathered schema columns, generated 2026-08-17 by scanning every CREATE TABLE / ADD COLUMN in supabase/migrations. surveillance-invariants.test.ts fails CI when a migration introduces a column name that is NOT in this list. The list may SHRINK freely as columns are dropped; growing it requires an explicit, reviewable diff to this file in the same PR as the migration. That review is the point: it is where someone asks whether a new per-student attribute should exist at all. Being on this list means grandfathered, not blessed.",
  "columns": [
    "INSERT",
    "ON",
    "abstraction_comfort",
    "acted_at",
    "active",
    "analysis",
    "answer",
    "answer_text",
    "answered_at",
    "approved_at",
    "approved_by",
    "arbitrator_version",
    "artifacts_count",
    "assigned_at",
    "atom_id",
    "atom_ids",
    "atom_spec",
    "atom_type",
    "attempts",
    "avatar_url",
    "awarded_at",
    "bytes",
    "candidate_version_n",
    "canonical",
    "canonical_at",
    "canonical_reason",
    "chunk_index",
    "cohort_key",
    "collected_at",
    "collection",
    "completed",
    "completed_at",
    "completion_rate",
    "computed_at",
    "concept_id",
    "concept_ids",
    "concept_pattern",
    "confidence",
    "confidence_before",
    "confidence_calibration",
    "config",
    "consecutive_failures",
    "content",
    "content_id",
    "content_type",
    "contract_params",
    "contract_version",
    "control_version_n",
    "correct_answer",
    "correct_count",
    "corrective_hint",
    "cost_usd",
    "count",
    "created_at",
    "created_by",
    "current_streak",
    "custom_id",
    "date",
    "deadline_at",
    "decisions",
    "delta_days",
    "demotions",
    "dependent_id",
    "description",
    "diagnosis",
    "diagnostic_scores",
    "diagnostic_taken_at",
    "difficulty",
    "difficulty_base",
    "digest_md",
    "display_name",
    "distractor_failure_tags",
    "distractors",
    "document_id",
    "due_at",
    "duration_ms",
    "dwell_fit",
    "email",
    "empirical_difficulty",
    "enabled",
    "ended_at",
    "endpoint",
    "ends_at",
    "error",
    "error_log",
    "error_pct",
    "error_tag",
    "error_type",
    "errored",
    "evaluated_at",
    "event_type",
    "exam",
    "exam_date",
    "exam_id",
    "exam_key",
    "exam_pack_id",
    "exam_strategy",
    "exam_tags",
    "excerpt",
    "expected_lift",
    "expected_n",
    "experiment_id",
    "experiments_evaluated",
    "expires_at",
    "explanation",
    "file_size",
    "filter_exam_id",
    "filter_topic",
    "final_grade",
    "first_seen_at",
    "flags",
    "format_instructions",
    "frustration_threshold",
    "fsrs_due",
    "gap_text",
    "gate_frequency",
    "generated_at",
    "generation_meta",
    "generation_run_id",
    "git_sha",
    "google_sub",
    "graded_at",
    "hit_count",
    "html_content",
    "hypothesis",
    "id",
    "identifier",
    "identifier_type",
    "improvement_reason",
    "insufficient_count",
    "intent",
    "interactives_enabled",
    "interval_days",
    "is_owner",
    "item_id",
    "item_ids",
    "item_key",
    "keys",
    "kind",
    "label",
    "lapses",
    "last_active_date",
    "last_answer",
    "last_promoted_at",
    "last_quality",
    "last_recall_correct",
    "last_reverted_at",
    "last_review_at",
    "last_seen",
    "late",
    "latency_bucket",
    "latency_ms",
    "learning_objectives",
    "lift_n",
    "lift_p",
    "lift_updated_at",
    "lift_v1",
    "llm_agreement",
    "llm_tokens",
    "longest_streak",
    "mapping_id",
    "marking",
    "marks",
    "marks_earned",
    "marks_max",
    "mastery",
    "mastery_share",
    "mastery_vector",
    "match_count",
    "max_marks",
    "media_hints",
    "message",
    "meta_desc",
    "metadata",
    "min_similarity",
    "misconception_hash",
    "misconception_id",
    "month_start",
    "motivation_state",
    "n",
    "n_seen",
    "name",
    "negative_marks",
    "next_review",
    "object_id",
    "official_source_url",
    "options",
    "org_id",
    "org_name",
    "override_content",
    "owner_user_id",
    "page_count",
    "paper",
    "pattern_key",
    "payload",
    "pedagogical_action",
    "pedagogy_score",
    "phone",
    "plan_date",
    "platform",
    "pr_url",
    "prepared_for_pyq_ids",
    "prerequisite_alerts",
    "prerequisite_id",
    "priority",
    "priority_score",
    "problem",
    "problem_id",
    "processed_at",
    "promoted_count",
    "promotions",
    "proposed_grade",
    "published_at",
    "pyq_id",
    "quality",
    "query_text",
    "question_kind",
    "question_text",
    "questions",
    "quiz_id",
    "ran_at",
    "rated_at",
    "rating",
    "rating_score",
    "raw_data",
    "raw_user_meta_data",
    "reason",
    "reasoning",
    "record",
    "recorded_at",
    "regen_abandon_rate",
    "repetitions",
    "representation_mode",
    "reps",
    "requires_review",
    "resonance_v1",
    "result",
    "retrieval_schedule",
    "reverted_count",
    "reviewed_at",
    "reviewer_id",
    "reviewer_notes",
    "role",
    "rule_text",
    "run_id",
    "scaffold",
    "scheduled_at",
    "scope",
    "score",
    "sections",
    "selected_concept",
    "selected_difficulty",
    "sent_at",
    "seo_meta",
    "served_at",
    "session_id",
    "session_stat",
    "session_type",
    "shadow_mode",
    "signals",
    "similarity",
    "site",
    "skill_id",
    "skipped",
    "slug",
    "sm2_due",
    "solution_steps",
    "source",
    "source_experiment_id",
    "source_id",
    "source_page",
    "source_type",
    "source_url",
    "speed_profile",
    "src_path",
    "started_at",
    "state",
    "status",
    "strength",
    "student_answer",
    "student_id",
    "student_message",
    "student_model_snapshot",
    "student_response",
    "studymate_id",
    "submitted_at",
    "submitted_by",
    "suggestions",
    "superseded_by",
    "taken_at",
    "target_error_type",
    "target_id",
    "target_kind",
    "target_misconception",
    "target_score",
    "tasks",
    "template",
    "template_version",
    "tie_count",
    "tier1_ms",
    "tier2_ms",
    "tier3_ms",
    "tier_used",
    "time_limit_minutes",
    "time_taken_ms",
    "timing_mode",
    "times_correct",
    "times_served",
    "title",
    "token_count",
    "topic",
    "topic_confidence",
    "topic_family",
    "topic_match",
    "total_ms",
    "total_xp",
    "trace_id",
    "trigger_reason",
    "ts_ms",
    "unit_id",
    "updated_at",
    "url",
    "usd_estimate",
    "user_answer",
    "user_id",
    "utm_params",
    "variant",
    "variant_kind",
    "verdict",
    "verification_confidence",
    "verification_method",
    "verification_status",
    "verified",
    "verified_at",
    "verifier",
    "version_n",
    "view_count",
    "views",
    "was_correct",
    "weekly_hours",
    "why_tempting",
    "why_wrong",
    "window_days",
    "wolfram_calls",
    "working_memory_est",
    "xp_amount",
    "year"
  ],
  "$reviewed": {
    "collection": "043_durable_records — discriminator naming WHICH flat-file store a row came from ('retention-items', 'exams', 'teacher-announcements'). A table-name substitute, not an attribute of anybody. Structural.",
    "errored": "040_pedagogy_shadow_log — diagnostic flag on the Tier 4 shadow log, distinguishing \"the judge could not answer\" from \"the content scored zero\". Not student-scoped: the row is keyed by curriculum unit, carries no student or session id, and the table is explicitly truncatable.",
    "google_sub": "041_auth_user_records — Google subject claim. Personal identity data, but NOT new collection: it is already in .data/users.json on every deploy today. This migration makes existing data durable, it does not gather anything more.",
    "is_owner": "041_auth_user_records — single owner flag, replacing the file-level owner_id. A role bit, not a behavioural attribute.",
    "mapping_id": "042_durable_content_stores — which curriculum-to-exam mapping produced a unit. Content identifier, no person in it.",
    "org_id": "041_auth_user_records — tenant id, already present throughout the schema. Not per-student.",
    "priority": "042_durable_content_stores — feedback triage level (low/medium/high). Operator workflow metadata, set by a reviewer, not observed from a student.",
    "record": "041_auth_user_records — the whole User object as JSONB. A blob is a HOLE in a column-name gate: fields added inside it get none of this scrutiny. Closed by the User-field allowlist in src/__tests__/unit/auth/user-store-durability.test.ts, which fails when User grows a field nobody reviewed.",
    "scope": "043_durable_records — per-owner partition key, usually a student_id or user_id, so that mirroring one student's records cannot delete another's. This DOES associate rows with a person, and it is the sensitive column in 043. Kept because without it every write would have to rewrite the whole cohort's data, which both leaks more and scales worse. It is an identifier for partitioning only — nothing reads it as a signal about the student, and the surveillance invariants still forbid src/api/* and the frontend from importing the personalization scorers that would. The records themselves are the same objects already sitting in .data today: this migration makes existing data survive a restart, it does not collect anything new.",
    "submitted_by": "042_durable_content_stores — user_id of whoever filed a feedback item. This DOES link a person to what they reported, and it is the most sensitive column in 042. Kept because feedback triage is a conversation: an operator has to be able to follow up, and duplicate-detection needs to know two reports came from different people. NOT new collection — the field already exists in .data/feedback.json on every deploy. Promoted to a column only so listFeedback can filter on it.",
    "unit_id": "042_durable_content_stores — which content unit a generated body belongs to. Content identifier, no person in it.",
    "xp_amount": "046_xp_quiz — signed XP-minutes for one graded attempt (src/scoring/xp.ts xpForAttempt). Personal-only: never summed into any cohort/peer view (surveillance invariant 10/11), never individually surfaced when negative.",
    "result": "046_xp_quiz — the graded quiz breakdown (earned/max/per-item correctness), same shape already returned by POST /api/practice/attempt. Stored so a double-submit replays the SAME graded result instead of re-grading a possibly-different payload. Not a behavioural log: no timing telemetry beyond what the receipt already shows the student.",
    "item_ids": "046_xp_quiz — the ordered object_id list a started quiz committed to. Content identifiers only, no student attribute.",
    "late": "046_xp_quiz / 047_mock_exams — whether a submission landed after its deadline. Grading outcome flag, not a behavioural profile field.",
    "max_marks": "046_xp_quiz / 047_mock_exams — total possible marks for a graded session. Mirrors the existing baselined score column pairing.",
    "deadline_at": "046_xp_quiz — server-computed quiz/exam deadline (started_at + fixed budget). Not observed behaviour — set once at start, read-only after.",
    "graded_at": "046_xp_quiz / 047_mock_exams — when server-side grading completed. Operational timestamp, same class as the already-baselined submitted_at/started_at.",
    "awarded_at": "046_xp_quiz — when an xp_events row was written. Operational timestamp.",
    "quiz_id": "046_xp_quiz — reserved name (not a live column in the shipped 046 shape; quiz_sessions.id serves this role). Grandfathered defensively so a future FK column reusing the same name does not need a second review.",
    "total_xp": "046_xp_quiz — reserved name for a possible future cached-total column; today the total is computed on read (src/gbrain/xp-store.ts), never stored. Grandfathered defensively.",
    "exam_key": "047_mock_exams — which named exam config (gate/bitsat/...) a mock exam was drawn from. Content/config identifier, not a student attribute.",
    "questions": "047_mock_exams — the SERVER-ONLY full question set including each answer key. Never serialized to the client pre-submission (leak test in src/api/__tests__/mock-exam-routes.test.ts mirrors the practice-item pattern). Real migration replacing the prior runtime-SQL CREATE TABLE that bypassed this gate entirely (T22/ENG-D3).",
    "time_limit_minutes": "047_mock_exams — the exam configs own fixed duration (EXAM_CONFIGS). Config value, not observed behaviour.",
    "analysis": "047_mock_exams — the graded breakdown (earned/max/per-topic), mirroring 046 quiz_sessions.result. Stored so a double-submit replays the same graded analysis instead of re-grading a possibly-different payload.",
    "owner_user_id": "047_mock_exams — the authenticated user (user.userId) that generated a mock exam; the IDOR-closing identity for GET .../mock-exam/:sessionId and POST .../submit (src/api/mock-exam-routes.ts). This DOES link a person to an exam, and is the most sensitive column in 047. Kept because ownership binding is the entire point of the fix: without it any authenticated student could read or grade another student's exam by guessing/reusing a sessionId. Deliberately NOT the same as session_id (that stays the mastery-calibration key, which may legitimately differ from the auth id — see the migration file's header comment) and NOT read by anything cohort/peer-facing — it is compared 1:1 against the caller's own id, never surfaced or aggregated.",
    "concept_ids": "048_pyq_concept_ids — the full set of concepts one exam question covers (primary first), alongside the existing concept_id primary. Content classification, exactly like the already-baselined concept_id: it describes the QUESTION (which syllabus concepts it tests), not the student who answers it. No behavioural signal, no per-student value ever stored here — src/db/pyq-concept-mapper.ts derives it deterministically from author-curated tags on the static question bank, same 'never a guess' discipline as concept_id.",
    "timing_mode": "049_mock_exam_timing_mode — which named pacing mode (standard/compressed/rush) a mock exam was generated under, per docs/demo/buyer-qa-demo-prep.md's C2 follow-up. A content/config choice the student made before starting, mirroring the already-baselined exam_key — not an observed behaviour, and never aggregated across students.",
    "exam": "050_assessment_contracts — which exam a marking contract governs ('gate'). Exam identity, not a person: the table has no student or session column and every row is public exam-authority fact.",
    "paper": "050_assessment_contracts — which paper of the exam ('common-em'). Marking differs per paper (the EM section is marked differently from a full MA paper), so it is part of the key, not a detail of the slug. Exam structure, not a person.",
    "marking": "050_assessment_contracts — per-question-type {strategy, params} JSONB: which marking algorithm applies and the numbers it runs on. A JSONB blob is a HOLE in a column-name gate, so this one is additionally pinned two ways: src/exams/__tests__/assessment-contract-seed.test.ts parses the migration and fails when its values drift from the compiled module, and the loader (src/exams/assessment-contract-loader.ts) validates the shape at read time and falls back to the compiled contract on anything malformed. Marking rules only — a per-student field cannot appear inside it without both of those changing in the same diff.",
    "official_source_url": "050_assessment_contracts — the exam authority's published notification the numbers were transcribed from. Provenance for a public fact. Nullable on purpose: a fabricated URL that implies verification is worse than an honest NULL.",
    "verified_at": "050_assessment_contracts — when a human last reconciled the row against official_source_url. An operator-process timestamp about a DOCUMENT, not an observation of anybody.",
    "question_kind": "051_attempt_facts — 'mcq'|'msq'|'nat' for the graded item, NULL when the grading path has no such kind (rubric-graded descriptive responses). A property of the QUESTION, not of the person answering it; it sits on the attempt row only because that is where question and outcome meet. Read exclusively by the W1.6 mode-split guard, which groups by it across a cohort and never reads an individual row.",
    "marks_earned": "051_attempt_facts — signed marks this attempt actually earned, under the contract named in contract_version. Signed, not clamped: a wrong 2-mark question earns -2/3 and the ledger must agree with the receipt the student was already shown. Makes an already-visible number durable rather than observing anything new.",
    "marks_max": "051_attempt_facts — the item's maximum marks, stored beside marks_earned so an accuracy ratio can be computed without re-reading the item (whose marking may since have been re-authored). An item attribute, not a student one.",
    "skipped": "051_attempt_facts — whether the student submitted nothing for this item. This IS the column the table exists for: xp_events (046) writes no row at all for a skip, so \"the cohort got faster by skipping the hard ones\" is invisible in every existing table, and the W1.6 speed/accuracy guard cannot detect that gaming mode without it. Aggregate-only by construction — no route or view renders a per-student skip history.",
    "contract_version": "051_attempt_facts / 052_session_contract_pin — which versioned assessment contract's rules produced this grade ('gate-2026', or 'gate-2026+compiled' when the compiled constant answered because no row was read). Provenance of the RULES, not an attribute of the student: two attempts scored under different annual schemes must stay distinguishable years later. On mock_exams/quiz_sessions it is the pin written at creation (plan E7), so a re-notified scheme can never silently re-grade a session that was started under the old one.",
    "latency_bucket": "051_attempt_facts — one of four coarse labels ('lt10s'|'10-30s'|'30-90s'|'gt90s'), NULL when latency was not observed. Raw milliseconds are deliberately NOT stored. The only consumer is the W1.6 speed/accuracy guard, which compares a COHORT's mean bucket against a matched control cohort's inside an experiment window; it never reads a row, never attributes a bucket to a named student, and no route or view exposes the column per student. Four buckets answer \"did this cohort start rushing while getting more wrong?\"; milliseconds would additionally answer \"how long did this student hesitate on this question on this evening\", which nothing in the product needs and which is a behavioural trace rather than a measurement. The CHECK constraint on the column is what stops a future writer quietly putting a millisecond count into a TEXT column.",
    "contract_params": "052_session_contract_pin — the resolved assessment contract (its per-question-type {strategy, params}) snapshotted onto a mock_exams / quiz_sessions row at CREATION. The content of the marking rules; no person in it. Exists so grading reads the snapshot instead of re-resolving at submit: an operator correcting a live contract mid-session must not retroactively change the rules a student was already answering under."
  }
}
