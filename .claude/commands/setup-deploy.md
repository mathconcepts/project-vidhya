---
description: Configure deployment settings for /land-and-deploy. Detects your deploy 
---
Invoke the `setup-deploy` skill from gstack using the Skill tool. Pass any arguments the user provided after the slash command as the skill's args.

Skill name: `setup-deploy`
