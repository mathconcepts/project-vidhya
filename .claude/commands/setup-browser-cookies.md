---
description: Import cookies from your real Chromium browser into the headless browse session. 
---
Invoke the `setup-browser-cookies` skill from gstack using the Skill tool. Pass any arguments the user provided after the slash command as the skill's args.

Skill name: `setup-browser-cookies`
