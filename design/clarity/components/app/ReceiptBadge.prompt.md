Wraps verified content in a hairline green surface with a ✓ caption.

```jsx
<ReceiptBadge receipt={{ verified: true }} source="Wolfram">{answer}</ReceiptBadge>
```

The one inviolable rule of this system: if there is no receipt object, there is no border. Estimates never wear it.
