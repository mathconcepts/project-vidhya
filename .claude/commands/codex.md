---
description: OpenAI Codex CLI wrapper — three modes. Code review: independent diff review via 
---
Invoke the `codex` skill from gstack using the Skill tool. Pass any arguments the user provided after the slash command as the skill's args.

Skill name: `codex`
