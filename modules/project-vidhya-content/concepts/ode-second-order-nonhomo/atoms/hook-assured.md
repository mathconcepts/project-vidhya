---
# Alternative body for ode-second-order-nonhomo.hook, served when the
# learner stance is `assured`. The base file is what a steady student reads.
# See src/content/stance-variants.ts for how this is selected.
#
# Written for a student who already has the mechanics: terse, assumes the
# vocabulary, and spends its words on the distinctions that actually cost
# marks (degenerate cases, faster routes, common false generalisations)
# rather than re-teaching what they can already do.
id: ode-second-order-nonhomo.hook.assured
concept_id: ode-second-order-nonhomo
atom_type: hook
bloom_level: 1
difficulty: 0
exam_ids: ["*"]
variant_of: ode-second-order-nonhomo.hook
for_stance: assured
---

$y_p$ alone satisfies the forcing term, not the boundary or initial conditions. Applying those conditions before $y_h$ is added back in throws away exactly the constants meant to absorb them — a sequencing slip, distinct from anything wrong in $y_p$ itself.
