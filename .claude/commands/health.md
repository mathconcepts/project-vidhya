---
description: Code quality dashboard. Wraps existing project tools (type checker, linter, 
---
Invoke the `health` skill from gstack using the Skill tool. Pass any arguments the user provided after the slash command as the skill's args.

Skill name: `health`
