---
description: Codify the most recent successful /scrape flow into a permanent 
---
Invoke the `skillify` skill from gstack using the Skill tool. Pass any arguments the user provided after the slash command as the skill's args.

Skill name: `skillify`
