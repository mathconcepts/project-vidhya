Floating indigo button pinned above the tab bar.

```jsx
<TutorFab onClick={openTutor}><MessageIcon /></TutorFab>
```

Indigo is reserved for the tutor — this button is the reason the reservation exists. Hide it on the tutor screen itself.
