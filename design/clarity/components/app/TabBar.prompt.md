The app shell's bottom navigation.

```jsx
<TabBar items={[{value:'home',label:'Today'},{value:'notes',label:'Notes'},{value:'progress',label:'Progress'}]} value={tab} onChange={setTab} />
```

Keep it at three tabs. Content scrolls under the material — do not make it opaque.
