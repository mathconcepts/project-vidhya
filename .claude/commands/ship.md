---
description: Ship workflow: detect + merge base branch, run tests, review diff, bump VERSION, 
---
Invoke the `ship` skill from gstack using the Skill tool. Pass any arguments the user provided after the slash command as the skill's args.

Skill name: `ship`
