---
description: Pair a remote AI agent with your browser. One command generates a setup key and 
---
Invoke the `pair-agent` skill from gstack using the Skill tool. Pass any arguments the user provided after the slash command as the skill's args.

Skill name: `pair-agent`
