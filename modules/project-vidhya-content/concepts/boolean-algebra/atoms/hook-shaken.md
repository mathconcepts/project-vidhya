---
# Alternative body for boolean-algebra.hook, served when the learner stance is
# `shaken`. The base file is what a steady student reads.
# See src/content/stance-variants.ts for how this is selected.
#
# Written for a student who is low on this concept and low on confidence:
# smallest true first step, concrete numbers before symbols, picture before
# formula, and the check made explicit. No praise, no reassurance, and no
# mention of how the reader might be feeling — a small win is what steadies
# someone, not being told they are struggling.
id: boolean-algebra.hook.shaken
concept_id: boolean-algebra
atom_type: hook
bloom_level: 1
difficulty: 0
exam_ids: ["*"]
variant_of: boolean-algebra.hook
for_stance: shaken
---

Set $A=1$ and $B=0$ in $F = AB$: multiply them, $F=0$. Now flip $B$ to $1$: $F=1$. Two switches, four possible settings, four output values — write out all four. One question sits underneath that column: can the same four values come from a shorter expression?
