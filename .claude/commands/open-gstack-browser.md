---
description: Launch GStack Browser — AI-controlled Chromium with the sidebar extension baked in. 
---
Invoke the `open-gstack-browser` skill from gstack using the Skill tool. Pass any arguments the user provided after the slash command as the skill's args.

Skill name: `open-gstack-browser`
