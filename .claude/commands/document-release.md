---
description: Post-ship documentation update. Reads all project docs, cross-references the 
---
Invoke the `document-release` skill from gstack using the Skill tool. Pass any arguments the user provided after the slash command as the skill's args.

Skill name: `document-release`
