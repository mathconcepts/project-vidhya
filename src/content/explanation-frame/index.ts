/**
 * Explanation Frame — barrel.
 *
 * Read `types.ts` first: it carries the problem statement, the two
 * guarantees (G1 floor, G2 monotonic enrichment) and the reason this is a
 * declared contract rather than a convention.
 */
export * from './types';
export * from './contract';
export * from './resolvers';
export * from './compose';
export * from './build';
