# Concept anchors — discrete-mathematics
#
# ONE plain sentence per concept, rendered at the top of the first lesson
# card before any mathematics. Contract v1 (see
# docs/designs/2026-09-18-concept-anchor-and-rendering-agenda.md §4):
# <=100 characters (~3 rendered lines at 375px), a concrete named system,
# carries the bridge not a name-drop, no notation, no exam framing, true
# and direction-precise.
version: 1
topic: discrete-mathematics
concepts:
  boolean-algebra:
    anchor: "A chip designer simplifies a circuit's expression: fewer terms mean fewer physical logic gates."
  functions-combinatorics:
    anchor: "A test engineer counts every way 4 requests route to 2 servers, to make sure edge cases get tested."
  group-theory-basics:
    anchor: "A materials scientist classifies a crystal by rotations that leave it unchanged — a group's axioms."
  propositional-logic:
    anchor: "A firewall rule 'if traffic matches, block it' breaks only when matching traffic still gets through."
  recurrence-relations:
    anchor: "A bank computes next month's loan balance from a formula, not by replaying every past month in turn."
  sets-relations:
    anchor: "A database groups customers into zip-code classes; the matching rule must behave consistently."
