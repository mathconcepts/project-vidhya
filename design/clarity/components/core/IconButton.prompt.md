Icon-only control for headers and rows; always pass `label`.

```jsx
<IconButton label="Settings" filled><SettingsIcon /></IconButton>
```

Default 44px meets the HIG hit target even when the glyph is 20px. Never shrink below 44 in a nav bar.
