---
description: Cross-model benchmark for gstack skills. Runs the same prompt through Claude, 
---
Invoke the `benchmark-models` skill from gstack using the Skill tool. Pass any arguments the user provided after the slash command as the skill's args.

Skill name: `benchmark-models`
