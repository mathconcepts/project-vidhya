---
id: trigonometric-functions.hook-assured
concept_id: trigonometric-functions
atom_type: hook
bloom_level: 1
difficulty: 0.0
exam_ids: ["*"]
variant_of: trigonometric-functions.hook
for_stance: assured
---

Every JEE general-solution mark depends on one habit: apply $\theta=n\pi+(-1)^n\alpha$ only once the equation reads exactly $\sin(\text{something})=\sin\alpha$, then divide the WHOLE right side by whatever multiplies $\theta$ — not just $\alpha$. For $\sin2\theta=1/2$, the correct solution is $\theta=n\pi/2+(-1)^n\pi/12$: the $n\pi$ term gets halved too. Halve only $\alpha$ instead — $\theta=n\pi+(-1)^n\pi/12$ — and it silently fails half the time: at $n=1$, that gives $\theta=11\pi/12$, and $\sin(2\theta)=\sin(11\pi/6)=-0.5$, not $0.5$. The formula is not a template to paste symbols into; the substitution has to survive being checked.

```interactive-spec
{"v":1,"kind":"simulation","title":"Where sin(theta) = 0.5 on the unit circle — trace it once, count the crossings","why":"This animates why one \"solve for theta\" answer is incomplete: the same height on a circle is hit twice per turn, and infinitely often overall — exactly what the general solution formula packs into one line.","x_expr":"cos(t)","y_expr":"sin(t)","t_min":0,"t_max":6.28319,"duration_sec":7,"view_box":{"x_min":-1.3,"x_max":1.3,"y_min":-1.3,"y_max":1.3},"narration_steps":[{"at_progress":0,"text":"Follow the marked point once around this circle of radius 1, starting at $(1,0)$. Its height above the centre is $y=\\sin\\theta$. Before one full turn ends, how many times will that height hit exactly $0.5$?","text_shaken":"Follow the point around the circle from $(1,0)$. Its height is $\\sin\\theta$. Count how many times $\\sin\\theta=0.5$ before one full turn ends.","text_assured":"Predict before it's shown: a horizontal line at height $0.5$ meets one full turn of this circle how many times?"},{"at_progress":0.0833,"text":"At $\\theta=30°$ the point sits at $(0.87,0.5)$ — height exactly $0.5$. First crossing found.","text_shaken":"At $\\theta=30°$: point $(0.87,0.5)$. Height $=0.5$. That is one solution.","text_assured":"$\\theta=\\pi/6$ satisfies $\\sin\\theta=0.5$ — the reference angle every other solution builds from.","focus_point":true},{"at_progress":0.4167,"text":"At $\\theta=150°$, the point is at $(-0.87,0.5)$ — the same height again, mirrored across the vertical axis. Second crossing, and the circle is not done yet.","text_shaken":"At $\\theta=150°$: point $(-0.87,0.5)$. Same height $0.5$. Second crossing.","text_assured":"$\\sin(180°-30°)=\\sin30°$ — the supplementary angle always repeats sine's value. This is where the $(-1)^n$ in the general formula comes from.","focus_point":true,"emphasize":true},{"at_progress":0.7,"text":"Past $\\theta=252°$ the point sits well below the centre line — no third crossing is coming. One full turn gives exactly two solutions.","trap":{"text":"Students solve $\\sin\\theta=0.5$, find $\\theta=30°$, and stop there — treating one angle as the whole answer when the equation actually has infinitely many solutions.","avoid":"State the FULL general solution, $\\theta=n\\pi+(-1)^n(\\pi/6)$, even when the question only says \"solve\" — a lone angle is an incomplete answer on JEE."}},{"at_progress":0.97,"text":"Two solutions per turn, repeating every $2\\pi$: the general solution packs both into $\\theta=n\\pi+(-1)^n\\dfrac{\\pi}{6}$ — $n=0$ gives $30°$, $n=1$ gives $150°$, and every later turn comes free.","text_shaken":"Two solutions each turn. Formula: $\\theta=n\\pi+(-1)^n\\pi/6$. $n=0$ gives $30°$. $n=1$ gives $150°$. It repeats forever.","text_assured":"$\\theta=n\\pi+(-1)^n\\pi/6$ is not a trick — read it as \"turn by $n$ half-turns, then correct by $\\pm\\pi/6$ depending on whether $n$ is odd or even.\"","emphasize":true}]}
```
