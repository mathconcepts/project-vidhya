---
description: Fast headless browser for QA testing and site dogfooding. Navigate any URL, interact with 
---
Invoke the `browse` skill from gstack using the Skill tool. Pass any arguments the user provided after the slash command as the skill's args.

Skill name: `browse`
