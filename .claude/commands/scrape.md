---
description: Pull data from a web page. First call on a new intent prototypes the flow 
---
Invoke the `scrape` skill from gstack using the Skill tool. Pass any arguments the user provided after the slash command as the skill's args.

Skill name: `scrape`
