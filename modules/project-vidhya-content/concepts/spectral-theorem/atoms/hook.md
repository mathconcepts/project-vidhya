---
id: spectral-theorem.hook
concept_id: spectral-theorem
atom_type: hook
bloom_level: 1
difficulty: 0.0
exam_ids: ["*"]
---

Every symmetric matrix hides a secret: it can be perfectly decomposed into orthogonal directions where it acts like pure scaling. This is the Spectral Theorem — and it unlocks matrix functions like $\sqrt{A}$, $A^{100}$, and $e^A$ that would otherwise be intractable.

```interactive-spec
{"v":1,"kind":"simulation","title":"The ellipse's own axes are the eigenvectors — only when A is symmetric","x_expr":"3*cos(t)+sin(t)","y_expr":"cos(t)+3*sin(t)","t_min":0,"t_max":6.28319,"duration_sec":7,"view_box":{"x_min":-4.3,"x_max":4.3,"y_min":-4.3,"y_max":4.3},"narration_steps":[{"at_progress":0.0,"text":"This traces the image of the unit circle under the symmetric matrix $A=\\begin{pmatrix}3&1\\\\1&3\\end{pmatrix}$ — every point on the circle, pushed through $A$.","text_shaken":"$A=\\begin{pmatrix}3&1\\\\1&3\\end{pmatrix}$ applied to $(\\cos\\theta,\\sin\\theta)$ traces this curve. At $\\theta=0$: the image is $(3,1)$.","text_assured":"The image of the unit circle under $A$ — an ellipse whose axes ARE the eigenvectors, because this $A$ is symmetric.","emphasize":false},{"at_progress":0.125,"text":"At 45°, the trace reaches exactly 4 units out, along $(1,1)/\\sqrt2$ — that direction is an eigenvector, its eigenvalue 4.","text_shaken":"At $\\theta=45°$, the image is $(2.83,2.83)$, length exactly 4 — the eigenvalue for direction $(1,1)$.","text_assured":"Eigenvalue 4 along $(1,1)/\\sqrt2$: the semi-major axis. For a symmetric matrix this axis is exact, not approximate.","emphasize":true},{"at_progress":0.375,"text":"A quarter-turn further, along the PERPENDICULAR direction $(1,-1)/\\sqrt2$, the reach is exactly 2 — the other eigenvector, eigenvalue 2. The two are exactly 90° apart.","text_shaken":"At $\\theta=135°$, the image is $(-1.41,1.41)$, length exactly 2 — check: $(1,1)\\cdot(1,-1)=0$.","text_assured":"Eigenvalue 2 along $(1,-1)/\\sqrt2$ — perpendicular to the first, not by coincidence: $Q^{\\mathrm T}Q=I$ forces it.","emphasize":true},{"at_progress":0.6,"text":"Compare the dashed circle: a non-symmetric matrix like a pure rotation has no such directions at all — every angle looks identical, radius 1 everywhere, because its eigenvalues are complex.","text_shaken":"For $R=\\begin{pmatrix}0&1\\\\-1&0\\end{pmatrix}$, every image has length 1, at every $\\theta$. Solve $\\det(R-\\lambda I)=\\lambda^2+1=0$ — no real solutions.","text_assured":"A non-symmetric matrix's eigenvectors need not be perpendicular, or even real — a rotation like $R$ has none at all, only complex ones.","trap":{"text":"Students expect any matrix's eigenvectors to line up with its ellipse's axes like this one does.","avoid":"Verify $A^{\\mathrm T}=A$ first — only symmetric matrices get real, perpendicular eigen-axes."}},{"at_progress":1.0,"text":"Back where it started — because $A^{\\mathrm T}=A$, its eigenvectors are guaranteed real and perpendicular. That guarantee is the entire content of the Spectral Theorem.","text_shaken":"Back at $(3,1)$. $A^{\\mathrm T}=A$ is the one condition that guarantees this whole picture holds.","text_assured":"$A=Q\\Lambda Q^{\\mathrm T}$ works because $Q^{-1}=Q^{\\mathrm T}$ for orthogonal $Q$ — exactly what symmetry buys, and what the general $Q\\Lambda Q^{-1}$ does not assume."}],"ghost":{"x_expr":"cos(t)","y_expr":"sin(t)"}}
```
