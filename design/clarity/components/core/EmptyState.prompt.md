Shown when a list has nothing in it yet.

```jsx
<EmptyState title="Nothing here yet" body="Your notes appear as you practise." action={<Button variant="tinted" tone="tutor">Ask the tutor</Button>} />
```

Every empty state links somewhere — usually the tutor.
