---
description: Restore working context saved earlier by /context-save. Loads the most recent 
---
Invoke the `context-restore` skill from gstack using the Skill tool. Pass any arguments the user provided after the slash command as the skill's args.

Skill name: `context-restore`
